{
    "name": "cra-workspaces",
    "version": "0.1.0",
    "scripts": {
        "apps:build": "cd apps/cra && yarn run build",
        "packages:test": "jest --coverage"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/jest": "^22.1.3",
        "@types/node": "^9.4.0",
        "jest": "^22.4.0",
        "jest-environment-jsdom": "^22.4.0",
        "ts-jest": "^22.0.4",
        "typescript": "2.6.2"
    },
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "engines": {
        "node": ">= 7.9"
    }
}
