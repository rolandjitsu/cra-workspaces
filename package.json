{
    "name": "cra-workspaces",
    "version": "0.1.0",
    "scripts": {
        "build": "yarn --cwd apps/cra build",
        "start": "yarn --cwd apps/cra start",
        "test": "yarn --cwd apps/cra test"
    },
    "dependencies": {},
    "devDependencies": {
        "typescript": "~2.9.2"
    },
    "private": true,
    "workspaces": [
        "apps/*"
    ]
}
