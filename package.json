{
    "name": "cra-workspaces",
    "version": "0.1.0",
    "scripts": {
        "build": "yarn --cwd apps/cra build",
        "start": "yarn --cwd apps/cra start",
        "test": "yarn --cwd apps/cra test"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^9.4.0",
        "typescript": "2.8.3"
    },
    "private": true,
    "workspaces": [
        "apps/*"
    ]
}
